// decribe("Show test", function () {
//     it("should be correct object format on searchShows()", function () {
//         let shows = await searchShows('try');
//         expect(Object(shows[0].keys())).toEqual(["id", "name", "summary", "image"])
//     });
// });
